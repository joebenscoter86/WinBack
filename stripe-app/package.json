{
  "name": "winback-stripe-app",
  "version": "1.1.0",
  "description": "WinBack — Guided dispute resolution for Stripe merchants",
  "scripts": {
    "build": "stripe apps build",
    "start": "stripe apps start",
    "upload": "stripe apps upload"
  },
  "dependencies": {
    "@stripe/ui-extension-sdk": "^9.1.0",
    "stripe": "^17.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "typescript": "^5.5.0"
  }
}
